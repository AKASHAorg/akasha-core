import * as React from 'react';

export interface EnsEditPageProps {
  sdkModules: any;
  globalChannel: any;
  logger: any;
}

const EnsEditPage: React.FC<EnsEditPageProps> = () => {
  return <div>ENS Edit Page</div>;
};

export default EnsEditPage;
