#name: deploy ethereum.world
#
#on:
#  push:
#    branches:
#      - master
#    paths:
#      - 'sdk-packages/**'
#      - 'apps/**'
#      - 'ui/**'
#      - 'examples/**'
#      - 'scripts/**'
#      - 'Dockerfile'
#      - '.github/workflows/**'
#jobs:
#
#  build:
#    name: Build
#    runs-on: ubuntu-latest
#    steps:
#      - name: executing remote ssh commands using password
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.HOST }}
#          username: ${{ secrets.USERNAME }}
#          password: ${{ secrets.PASSWORD }}
#          port: ${{ secrets.PORT }}
#          script: whoami
