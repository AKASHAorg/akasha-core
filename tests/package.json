{
  "name": "@akashaproject/tests",
  "version": "0.0.1",
  "scripts": {
    "ethereum-world-local": "cypress open --project ethereum-world --env configFile=local",
    "ethereum-world-staging": "cypress open --project ethereum-world --env configFile=staging",
    "ethereum-world-prod": "cypress open --project ethereum-world --env configFile=production"
  },
  "devDependencies": {
    "cypress": "8.7.0",
    "fs-extra": "10.0.0"
  }
}
