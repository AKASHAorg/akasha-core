import * as initSdk from '@akashaproject/sdk';
import createSimpleModule from '../src/index';

test('placeholder', () => {
  // tslint:disable-next-line:no-console
  console.log('aaaaaaaa', initSdk);
  // const sdk = initSdk();
  // const simpleModule = createSimpleModule();
  // const api = sdk.start(simpleModule);
  // // tslint:disable-next-line:no-console
  // console.log(api);
  // expect(api).toHaveProperty('simple-module');
});
