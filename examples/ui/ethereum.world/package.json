{
  "name": "@akashaproject/ethereum.world",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@akashaproject/ui-awf-typings": "*",
    "@types/react": "17.0.0",
    "@types/react-dom": "17.0.0"
  },
  "scripts": {
    "build": "webpack --config ./webpack.config.js",
    "postbuild": "copyfiles -u 1 ./dist/* './dist/icons/*' ../../../ui/build/",
    "watch": "nodemon",
    "clean": "rm -rf dist"
  },
  "nodemonConfig": {
    "ignore": [
      "lib/*"
    ],
    "exec": "npm run build || exit 1",
    "ext": "ts,tsx"
  },
  "dependencies": {},
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "development": [
    "last 1 chrome version",
    "last 1 firefox version",
    "last 1 safari version"
  ]
}
