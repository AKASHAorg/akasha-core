export declare const capitalize: (str?: string | undefined) => string;
export declare const truncateMiddle: (
  str: string,
  startChars?: number,
  endChars?: number,
) => string;
/**
 * Check if is base64 encoded...
 * Simple check
 */
export declare const isBase64: (str: string) => boolean;
export declare const isBlob: (str: string) => boolean;
