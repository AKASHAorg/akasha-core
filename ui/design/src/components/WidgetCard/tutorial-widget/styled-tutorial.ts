import styled from 'styled-components';
import Button from '../../Button';
import { Box } from 'grommet';

const StyledButton = styled(Button)`
  width: 50%;
`;

const StyledImageBox = styled(Box)`
  width: 9.4rem;
  height: 9.4rem;
`;

export { StyledButton, StyledImageBox };
